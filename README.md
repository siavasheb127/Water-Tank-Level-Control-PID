# پروژه کنترل سطح مخزن آب (سیستم غیرخطی) 💧
**شبیه‌سازی، تحلیل پایداری و طراحی کنترلر PID**

این پروژه به منظور مدل‌سازی فیزیکی، تحلیل رفتار دینامیکی و طراحی سیستم کنترل برای یک مخزن آب غیرخطی (مدل توریچلی) در محیط MATLAB/Simulink انجام شده است.

## 📝 شرح پروژه (مطابق با فازهای اجرایی)

### فاز ۱: بازسازی مدل و شناسایی سیستم
در این فاز، به جای استفاده از مدل‌های آماده، سیستم بر اساس معادلات دیفرانسیل فیزیکی (قانون توریچلی) مدل‌سازی شد. سپس با استفاده از داده‌های استخراج شده در Workspace، تابع تبدیل خطی سیستم حول نقطه کار محاسبه گردید.

### فاز ۲: تحلیل پایداری و ویژگی‌های سیستم
در این مرحله، پایداری و رفتار ذاتی سیستم در حالت حلقه باز مورد بررسی قرار گرفت:
* **مرتبه و تایپ سیستم:** تعیین هویت ریاضی سیستم (مرتبه اول و تایپ صفر).
* **تحلیل فرکانسی:** رسم نمودارهای Bode و Nyquist برای بررسی حاشیه پایداری.
* **تحلیل زمانی:** بررسی پاسخ سیستم به ورودی‌های پله (Step)، ضربه (Impulse) و شیب (Ramp).

### فاز ۳: طراحی کنترلر PID
هدف نهایی، رسیدن به زمان نشست کمتر از ۵ ثانیه و فراجهش بین ۱۰ تا ۲۰ درصد بود. در این راستا کنترلرهای زیر پیاده‌سازی و مقایسه شدند:
* **کنترلر P و PD:** جهت بررسی سرعت پاسخ.
* **کنترلر PI:** جهت حذف خطای حالت ماندگار.
* **کنترلر PID:** تنظیم نهایی ضرایب برای دستیابی به بهترین عملکرد و پایداری.

---

## 📂 راهنمای فایل‌های موجود در مخزن (File Descriptions)

در این مخزن، فایل‌ها به شرح زیر دسته‌بندی شده‌اند تا دسترسی به آن‌ها آسان باشد:

* **📁 Simulink_Models:**
    * شامل فایل‌های `.slx` که مدل‌های شبیه‌سازی فاز ۱، فاز ۲ و مدل نهایی مجهز به کنترلر PID در آن قرار دارد.
* **📁 Scripts:**
    * شامل کدهای متلب (`.m`) جهت تعریف پارامترهای اولیه، رسم نمودارهای بود و نایکوئیست، و شناسایی سیستم.
* **📁 Reports:**
    * **Final_Report.pdf:** گزارش کامل پروژه که با نرم‌افزار LaTeX تهیه شده و شامل تمام تحلیل‌ها و نمودارهاست.
    * **Presentation.pptx:** فایل پاورپوینت جهت ارائه کلاسی پروژه.
* **📁 LaTeX_Source:**
    * کدهای منبع لایتک برای کسانی که تمایل دارند از قالب گزارش استفاده کنند.

---

## 🛠 نحوه استفاده (How to Run)
1. فایل‌های مربوط به شبیه‌سازی را در محیط MATLAB باز کنید.
2. با اجرای اسکریپت‌های موجود در پوشه Scripts، پارامترهای سیستم را در Workspace بارگذاری کنید.
3. مدل سیمولینک نهایی را اجرا کرده و نتایج را در Scope مشاهده نمایید.

---
**توسعه‌دهندگان:** [نام شما و هم‌گروهی]
